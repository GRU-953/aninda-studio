# Aninda Studio — continuous integration
#
# The jobs are ordered so the CHEAPEST FAILURE SURFACES FIRST. A typo fails in
# fifteen seconds; the browser job that takes several minutes never starts. That
# ordering is the whole design of this file.
#
# The job that matters most is `tokens`. It regenerates every generated file and
# then runs `git diff --exit-code`. A non-empty diff means one of two things: a
# generator changed without its output being regenerated, or somebody hand-edited
# an output file instead of its input. Both are silent failures in every other
# kind of check, and both have happened to real projects.
#
# That check only works because generation is deterministic — which is why nothing
# generated in this repository contains a timestamp. Build time lives in the CI
# log, never in a file that gets diffed.

name: brand

on:
  push:
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  PLAYWRIGHT_BROWSERS_PATH: ${{ github.workspace }}/00_sandbox/browsers

jobs:
  # 1 ── seconds. Catches the things that need no build at all.
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'
      - name: Every JSON parses
        run: |
          find . -name '*.json' -not -path './00_sandbox/node_modules/*' \
                 -not -path './.venv/*' -print0 \
          | xargs -0 -I{} python3 -c "import json,sys; json.load(open('{}'))"
      - name: No .DS_Store committed
        run: |
          if find . -name '.DS_Store' -not -path './.git/*' | grep -q .; then
            echo "::error::.DS_Store files are committed"; exit 1
          fi
      - name: Generated files still say they are generated
        run: |
          for f in 07_tokens/css/tokens.css; do
            grep -q "GENERATED" "$f" || { echo "::error::$f lost its header"; exit 1; }
          done
      - name: The English standard's blocklist is enforced
        # ENGLISH-STANDARD.md told the reader "every banned word and idiom above is
        # in a blocklist the checker enforces". The checker existed and no job ran
        # it, so nobody looked at the prose — and the standard itself used two
        # banned words, with seven more in 06_type. The list below is the scope the
        # standard now names: the authored English prose. It is a list rather than
        # the whole tree because the checker skips a file over 1 MB, and a generated
        # card is larger than that; those are held to the rule by their generators,
        # which read this prose rather than writing their own.
        run: |
          fail=0
          for path in README.md README.bn.md NOTICE TRADEMARKS.md LICENSE-DOCS.md                       00_sandbox/TOOLCHAIN.md 01_research 02_strategy                       09_guidebook/chapters 13_plugins/claude-code/README.md                       13_plugins/claude-code/skills 13_plugins/figma/README.md                       06_type/BANGLA-STANDARD.md 06_type/BANGLA-STRINGS.md                       06_type/SHORTLIST.md 06_type/RECOMMENDATION.md                       06_type/MEASUREMENTS.md 06_type/pairings.md ; do
            python 13_plugins/claude-code/skills/aninda-review/scripts/check.py "$path"               || { echo "::error::$path fails the English standard"; fail=1; }
          done
          exit $fail

      - name: CI and scripts/verify-all.sh must run the same gates
        # Three pushes went out red, each on a different gate the local script was
        # missing, each found by pushing. A local pass has to mean what a green run
        # means, and that only holds if the two lists agree.
        run: python scripts/check_gates.py
      - name: The benchmark's acceptance table matches its verdict data
        # Section 7 held 28 criteria and 28 empty verdict cells: the document that
        # defines what counts as done had never been run against the finished kit.
        run: python scripts/benchmark.py --check
      - name: The findings register matches its own data
        # OPEN-FINDINGS.md is the document a reader trusts to say what is wrong
        # here. It was hand-written and drifted: of its 54 open entries, four were
        # already fixed and six were half right. It is generated now, and its
        # counts are counted.
        run: python scripts/findings.py --check
      - name: The derived figures in MEASUREMENTS.md match the measurements
        # MEASUREMENTS.md opens by saying every number in it is a measurement, and
        # nothing compared it to 06_type/_data/measurements.json. Three x/cap cells
        # disagreed, in opposite directions, so it was not a rounding convention.
        run: python scripts/check_measurements.py

      - name: Every Reserved Font Name claim matches the licence we ship
        # This claim was corrected twice by hand and was wrong both times. The
        # second review found it still wrong in 35 files, including the two NOTICE
        # files a redistributor of the plugin must carry. Two hand-corrections were
        # not enough because nothing checked.
        run: python scripts/check_licence_claims.py

  # 2 ── under a minute. The drift guard. This is the important one.
  tokens:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'
      - run: pip install -r requirements.txt
      - name: Regenerate the palette proofs
        run: python 05_colour/engine.py
      - name: Regenerate the token set
        run: python 07_tokens/build.py
      - name: Regenerate the stylesheet
        run: python 07_tokens/emit_css.py
      - name: Nothing generated may differ from what was committed
        run: |
          git diff --exit-code 05_colour/generated 07_tokens/build 07_tokens/css \
            || { echo "::error::A generated file differs. Either a generator changed
                 without regenerating its output, or an output was hand-edited."; exit 1; }

  # 3 ── about a minute. Shaping and geometry.
  marks:
    runs-on: ubuntu-latest
    needs: tokens
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'
      - run: pip install -r requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Rebuild the marks — the shaping gates run inside
        run: python 04_mark/build.py
      - name: The marks must not have changed
        # proof.svg is in the diff because it did not used to be generated at all.
        # 04_mark/proof.png was a hand-made image that captioned the Apple and
        # watchOS panels as square, unmasked masters long after the owner decided
        # on one rounded icon everywhere, and because no script wrote it this job
        # could not see the contradiction.
        run: git diff --exit-code 04_mark/svg 04_mark/manifest.json 04_mark/proof.svg

  # 4 ── a couple of minutes. Everything that has a --check mode.
  build:
    runs-on: ubuntu-latest
    needs: tokens
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'
      - uses: actions/setup-node@v6
        with:
          node-version: '26'
      - run: pip install -r requirements.txt
      - name: The READMEs must still describe the repository they are in
        run: python scripts/readme.py --check
      - run: python 12_packages/build.py --check
      - run: python 08_components/build.py --check
      # These four steps used to read `test ! -f X || python X`, so deleting or
      # renaming any one of them turned it into a green pass on six jobs while the
      # artefact went unmeasured. All four scripts are committed, so the guard
      # could only ever hide their disappearance — which is the thing most worth
      # failing on. A check that did not run must never read as a pass.
      - name: The icon and social assets must match the marks
        # This build had no --check mode at all until 19 August 2026, so twenty
        # rasters, an .ico and a manifest were regenerated and diffed nowhere.
        run: python 10_assets/build.py --check
      - name: The committed PDF must still say what the book says
        # A byte diff is impossible: Chromium stamps a creation date, so two runs of
        # the same input differ. Measured. So this compares the PDF's TEXT against
        # the print build's headings — the shipped PDF was three weeks stale, still
        # claiming three fonts after the licence chapter was corrected to four, and
        # nothing could see it.
        run: python 09_guidebook/scripts/pdf.py --check
      - run: python 11_site/build.py --check
      - run: python 09_guidebook/build.py --check
      - name: The npm package must actually resolve
        # This step used to check that each export target EXISTS and then load
        # the CommonJS half. index.mjs was a link-time SyntaxError — `tokens` was
        # never declared in it — and this step passed on every commit, because
        # the file it stat-ed and the file it loaded were not the same file.
        # Both halves are now imported for real, and the values they return are
        # compared, so an entry point that parses but exports the wrong thing
        # also fails here.
        run: |
          cd 12_packages/npm
          node -e "
            const p = require('./package.json'), fs = require('fs');
            for (const [k, v] of Object.entries(p.exports)) {
              const f = typeof v === 'string' ? v : (v.import || v.types);
              if (!fs.existsSync(f)) { console.error('missing export target', k, f); process.exit(1); }
            }
          "
          node --input-type=module -e "
            import a from 'node:assert';
            import { createRequire } from 'node:module';
            import esm, { tokens, themes } from './dist/index.mjs';
            const cjs = createRequire(import.meta.url)('./dist/index.cjs');
            a.ok(esm && typeof esm === 'object', 'the ESM default export is not an object');
            a.strictEqual(tokens, esm, 'the ESM named and default exports disagree');
            a.ok(Array.isArray(themes) && themes.length === 4, 'themes is not the four themes');
            a.deepStrictEqual(Object.keys(cjs.tokens), Object.keys(esm),
              'the CommonJS and ESM entry points return different tokens');
            console.log('both entry points import, and agree on', Object.keys(esm).length, 'top-level keys');
          "

  # 5 ── under two minutes. The two plugins, and the two drift guards on them.
  #
  # The Figma plugin commits its dist/ folder, because Figma loads built
  # JavaScript and asking the owner to run a build before opening Figma is one
  # more place to get stuck. Committed build output drifts unless something
  # watches it, so this job rebuilds and diffs, exactly as the tokens job does.
  #
  # build.mjs runs with --code-only here. That flag compiles the artefacts and
  # stops before the manifest gate. It is not a way to ship an unadopted
  # manifest — the manifest is not committed at all, because it holds a plugin
  # id issued to one person, and CI has no way to obtain one.
  plugins:
    runs-on: ubuntu-latest
    needs: tokens
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'
      - uses: actions/setup-node@v6
        with:
          node-version: '26'
      - name: Install the project's own build tools
        run: npm ci
        working-directory: 00_sandbox
      - name: Install the Python dependencies
        # check_plugin.py reads the bundled Bangla subset's cmap with fontTools to
        # prove the font can draw the Bangla the skill ships — the guard that once
        # found ঠ and ২ missing, both of them in strings the skill cites as its own
        # authority. Without fontTools that check reports WRONG rather than
        # pretending, which is right, and this job never installed it. It was
        # latent: the step before it was failing first, so this one never ran.
        run: pip install -r requirements.txt
      - name: The Figma plugin must typecheck
        run: ../../00_sandbox/node_modules/.bin/tsc --noEmit -p tsconfig.json
        working-directory: 13_plugins/figma
      - name: Rebuild the Figma plugin
        run: node build.mjs --code-only
        working-directory: 13_plugins/figma
      - name: Nothing built may differ from what was committed
        run: |
          git diff --exit-code 13_plugins/figma/dist \
            13_plugins/figma/src/tokens.generated.ts \
            13_plugins/figma/RECEIPT-EXPECTED.json \
            || { echo "::error::The committed Figma plugin build differs from a
                 fresh one. Run 'node build.mjs --code-only' and commit dist/."; exit 1; }
      - name: A placeholder manifest must stop the build
        run: |
          cd 13_plugins/figma
          printf '{"id":"x","api":"REPLACE_ME__FIGMA_GENERATES_THIS"}\n' > manifest.json
          if node build.mjs > /dev/null 2>&1; then
            echo "::error::The manifest gate let a placeholder through"; rm -f manifest.json; exit 1
          fi
          rm -f manifest.json
      - name: The Claude Design bundle must match the system
        # The only deliverable whose artefact lives on a remote service, so the
        # only one that can fall behind with nothing saying so. Generated from the
        # same token files as everything else, and diffed here.
        run: python 13_plugins/claude-design/build.py --check
      - name: The Claude Code plugin must be well formed
        run: python 13_plugins/claude-code/scripts/check_plugin.py
      - name: The skill bundles must be reproducible
        run: python 13_plugins/claude-code/scripts/build_skills.py --prove
      - name: Nothing bundled may differ from what was committed
        run: git diff --exit-code 13_plugins/claude-code/dist

  # 6 ── slowest, and last. Its failures are usually symptoms of jobs 1 to 5.
  render:
    runs-on: ubuntu-latest
    needs: [marks, build]
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'
      - run: pip install -r requirements.txt
      - run: python -m playwright install --with-deps chromium
      - name: Measure the tokens in a real browser
        run: python 00_sandbox/measure.py
      - name: Measure every component card
        run: python 08_components/check.py
      - name: Measure the site
        run: python 11_site/check.py
