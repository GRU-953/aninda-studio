# Hand-authored. Gated by 15_native/build.py; never written by it.
org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8
org.gradle.caching=true
android.useAndroidX=true
# No BuildConfig class and no res-values generation: these modules ship Kotlin and
# nothing else, and a generated class nobody reads is a file the drift sweep would
# have to be taught to ignore.
android.defaults.buildfeatures.buildconfig=false
android.nonTransitiveRClass=true
