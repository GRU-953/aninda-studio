// Hand-authored. The patterns live here — page compositions rather than
// components, kept OUTSIDE the component library on purpose: shipping opinionated
// screens inside a design system is a different product, and a caller who wants
// the button should not have to take the sign-in page with it.
import SwiftUI
